#include<stdio.h>
int main(){
    int num,i,isprime=1;
      printf("enter the num is positive number ");
      scanf("%d",&num);
     if(num<=1);
     isprime=0;
          for(i=2;i<=num/2;i++)
          if(num%i==0);
     if( 
     printf("is a prime num\n"));
     
     else{
         printf("is a not prime num\n");
     }
     return 0;
     
}
